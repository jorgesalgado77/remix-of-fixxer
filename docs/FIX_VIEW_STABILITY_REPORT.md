Corrigi o erro de conexão causado pela ausência da coluna `created_at` na View `profiles_public` do Supabase, implementando uma seleção defensiva de colunas (Safe Selection) e fallback automático nos carrosséis de Lojistas e Parceiros. Realizei uma auditoria completa nos componentes afetados para garantir que a interface permaneça operacional mesmo durante atualizações parciais do esquema do banco de dados, priorizando a estabilidade visual e a resiliência dos dados.