// Neutralizado: qualquer bypass baseado em localStorage/email hardcoded
// foi removido. A autoridade de sessão é 100% Supabase.
// Mantido apenas como no-op para preservar imports existentes.
export const fixAuthAndPreview = () => {
  /* no-op */
};
